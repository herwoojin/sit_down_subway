# 앉아가요 (Anja-gayo) — 제품 기획 문서 (PRD)

> 지하철 빈자리 정보를 **좌석을 직접 클릭**해서 공유하는 실시간 서비스
> 원본 서비스 '저내려요(jeo-naeryeo.web.app)'의 컨셉을 계승하고, 좌석 클릭 UI로 업그레이드한 버전

| 항목 | 내용 |
|---|---|
| 제품명 | 앉아가요 (Anja-gayo) |
| 한 줄 정의 | 내가 앉은 자리를 클릭해 "곧 비워요"를 알리고, 비는 자리를 지도처럼 보는 빈자리 공유 앱 |
| 기반 서비스 | 저내려요 (호차 + 하차역 입력 방식) |
| 핵심 차별점 | 텍스트 입력 → **지하철 모양 좌석 그리드 클릭** 방식으로 전환 |
| 타깃 | 출퇴근/통학 등 지하철 정기 이용자 |
| 플랫폼 | 모바일 웹 (PWA) 우선, 추후 네이티브 앱 |
| 핵심 가치 | 자동 인식 · 좌석 클릭 · 크레딧 보상 · 시니어 접근성(큰 글씨·음성) |
| 사회적 목표 | 양보 문화 속 "암묵지(눈치)"의 불편을 정보로 해소, 좌석 편중 완화 |
| 문서 버전 | v0.3 (로그인·테마·노선도·혼잡도 반영) |

---

## 1. 배경과 문제 정의

지하철에서 빈자리를 찾으려면 승객들이 두리번거리며 누가 곧 내릴지를 눈치로 파악해야 한다. 원본 서비스 '저내려요'는 승객들끼리 빈자리 정보를 직접 공유하도록 해 이 문제를 풀었다. 내가 탄 **호차**와 **내릴 역**을 입력하면, 곧 비게 될 자리의 위치를 미리 참고할 수 있다.

다만 원본은 좌석을 **텍스트/리스트 입력** 방식으로 등록한다. 실제로 내가 앉은 위치가 차량 어디쯤인지를 직관적으로 표현하기 어렵고, 정보를 보는 사람도 "몇 번째 문 근처, 어느 쪽"인지 머릿속에서 변환해야 한다.

**핵심 문제:** 좌석 위치 정보의 입력과 조회가 모두 비직관적이다. 위치를 "공간"이 아니라 "텍스트"로 다루기 때문이다.

---

## 2. 원본 '저내려요' 분석

### 2.1 기술/구조 (파악된 사실)

- Flutter 로 빌드된 **웹앱** (`my_app`), Firebase Hosting (`*.web.app`) 으로 배포
- 화면이 캔버스로 렌더링되는 모바일 우선 웹앱 (PWA 메타태그 존재: `mobile-web-app-capable`)
- 입력 흐름: **호차 선택 → 하차역 선택 → 빈자리 정보 공유/조회**

### 2.2 강점

- 예약이 아니라 "참고용 정보 공유"라는 가벼운 컨셉 — 책임/분쟁 부담이 적다
- 사용자가 직접 등록하는 크라우드소싱 구조 — 운영 비용이 낮다
- 출퇴근이라는 명확하고 반복적인 사용 맥락

### 2.3 한계 (= 업그레이드 기회)

| 한계 | 업그레이드 방향 |
|---|---|
| 좌석 위치를 텍스트/리스트로 입력 | 지하철 모양 좌석 그리드에서 **탭 한 번**으로 등록 |
| 내 위치를 공간적으로 표현 못 함 | 차량 도면 위 좌석 하이라이트 |
| 정보의 신선도(언제 비는지) 표현 약함 | "n분 후/다음 역에서 비움" 상태값 |
| 정확도가 등록자 성실도에 의존 | 신뢰도 점수·검증·자동 만료 |

---

## 3. '앉아가요' 핵심 컨셉

> **"내가 앉은 자리를 누르면, 곧 비는 자리가 지도에 뜬다."**

원본의 "곧 비는 자리를 미리 안다"는 가치는 유지하되, 입력과 조회를 모두 **지하철 좌석 도면 위의 탭 인터랙션**으로 바꾼다.

두 가지 사용자 역할이 한 화면에서 자연스럽게 전환된다.

- **공유자 (앉아 있는 사람):** 내가 앉은 좌석을 탭 → "곧 내려요" 표시. 내릴 역/예상 시간을 한 번 더 탭해 정확도를 높임.
- **탐색자 (서 있는 사람):** 같은 호차 도면에서 곧 비는 좌석이 색으로 보임 → 그 근처로 이동.

여전히 **예약이 아닌 참고용**이라는 원본 철학을 유지한다. (좌석을 점유/잠그지 않는다.)

---

## 4. 기능 정의

### 4.1 MVP (1차 출시 범위)

1. **노선·역·호차 선택**: 내가 탄 위치 컨텍스트 설정 (호선 → 하행/상행 → 호차)
2. **좌석 그리드 표시**: 단순화된 한 칸 도면 (아래 5장)
3. **좌석 탭 등록 (공유)**: 내 좌석 탭 → 상태 선택(다음 역 / 2~3정거장 후 / 곧)
4. **빈자리 조회 (탐색)**: 곧 비는 좌석 하이라이트 + 카운트다운/역 라벨
5. **자동 만료**: 등록자가 내릴 역을 지나면 자동으로 표시 해제
6. **로그인 없는 빠른 사용**: 익명/기기 기반 시작, 가입 강요 없음

### 4.2 확장 기능 (2차 이후)

- 신뢰도 점수 (정확히 비운 이력 누적), 신고/검증 버튼
- 위치 기반 자동 호차·역 추정 (선택적, 권한 동의 시)
- 혼잡도 히트맵, 칸별 빈자리 통계
- 즐겨찾는 출퇴근 경로 저장, 도착 알림
- 다국어 (관광객 대상)

### 4.3 명시적 비범위 (Non-goals)

- 좌석 예약·점유·잠금 기능 (분쟁 유발, 컨셉과 충돌)
- 특정 개인 식별/추적
- 결제 기능

---

## 5. 좌석 클릭 UI 설계 (단순화 그리드)

핵심 업그레이드 지점이다. 실제 한국 지하철 한 칸은 양쪽 롱시트(긴 의자)와 양 끝 교통약자석으로 구성된다. 이를 **단순화된 좌석 그리드**로 추상화한다.

### 5.1 단순화 원칙

- 차량 한 칸 = **위쪽 줄 / 아래쪽 줄** 두 개의 좌석 띠로 표현 (양쪽 롱시트)
- 좌석 띠 사이는 통로(승객이 서는 공간)
- 문(출입구)은 좌석 띠를 끊는 구분선으로 표시
- 양 끝 모서리는 **교통약자석/노약자석**으로 별도 색 구분
- 좌석 1개 = 탭 가능한 사각형 1개

### 5.2 단순화 그리드 도식 (한 칸)

```
   [열차 진행 방향 →]

문      문      문      문
┌──┐ ┌───────┐ ┌───────┐ ┌──┐
│약약│ │■■■■■■■│ │■■■■■■■│ │약약│   ← 위쪽 롱시트
└──┘ └───────┘ └───────┘ └──┘
            ( 통  로 )
┌──┐ ┌───────┐ ┌───────┐ ┌──┐
│약약│ │■■■■■■■│ │■■■■■■■│ │약약│   ← 아래쪽 롱시트
└──┘ └───────┘ └───────┘ └──┘
문      문      문      문

■ 일반석   약 교통약자석
```

> 좌석 수는 호선/차량 형식에 따라 다르므로(보통 한 칸 일반석 약 42~54석), 위 도식은 "7인석 × 4묶음 + 양 끝 약자석"의 대표 패턴을 단순화한 것이다. 실제 구현 시 호선별 좌석 배치 데이터를 메타로 둔다.

### 5.3 좌석 상태(색상) 정의

| 상태 | 색 | 의미 |
|---|---|---|
| 빈 좌석 정보 없음 | 회색 | 정보 미등록 (점유 여부 모름) |
| 곧 비움 — 다음 역 | 초록 | 다음 정차역에서 비움 |
| 곧 비움 — 2~3정거장 | 노랑 | 가까운 시일 내 비움 |
| 내 좌석 (공유 중) | 파랑 | 내가 등록한 내 자리 |
| 교통약자석 | 보라 테두리 | 양보 우선석 표시 |

### 5.4 인터랙션 흐름

- **공유자:** 좌석 탭 → 바텀시트 등장(상태: "다음 역 / 2~3정거장 후 / 곧") → 선택 → 해당 좌석 파랑 + 카운트다운 시작
- **탐색자:** 화면 진입 시 곧 비는 좌석이 초록/노랑으로 점멸 → 좌석 탭 시 "○○역에서 비워요" 툴팁
- **취소/수정:** 내 파랑 좌석 다시 탭 → 등록 취소 또는 시간 변경
- **자동 해제:** 등록한 하차역 통과 또는 타이머 만료 시 회색 복귀

### 5.5 접근성

- 색상만이 아니라 아이콘/라벨 병기 (색약 대응)
- 탭 타깃 최소 44×44pt, 한 손 조작 가능한 하단 배치
- 좌석 그리드 핀치 줌 지원

---

## 6. 사용자 화면 흐름 (User Flow)

```
[앱 진입]
   │
   ▼
[호선 선택] → [방향(상/하행) 선택] → [호차 선택]
   │
   ▼
[좌석 그리드 화면] ──────────────────────────┐
   │                                          │
   ├─ (앉아 있음) 내 좌석 탭 → 상태 선택 → 공유 등록
   │                                          │
   └─ (서 있음) 초록/노랑 좌석 확인 → 그쪽으로 이동
                                              │
                          [하차역 통과 시 자동 만료/초기화]
```

화면은 3개로 최소화한다: **① 컨텍스트 선택(호선/방향/호차) → ② 좌석 그리드(메인) → ③ 좌석 상세 바텀시트.**

---

## 7. 데이터 구조 설계 (초안)

```
Line(노선)
  - lineId, name, color
Car(호차 메타)
  - carId, lineId, carNumber
  - seatLayout: [ {seatId, type: normal|priority, side: top|bottom, group, index} ]
Station(역)
  - stationId, lineId, name, order

SeatReport(좌석 공유 기록)  ← 핵심 실시간 데이터
  - reportId
  - lineId, direction, carNumber, seatId
  - status: nextStation | within2to3 | soon
  - exitStationId        (선택: 어느 역에서 비우는지)
  - createdAt, expiresAt  (자동 만료)
  - deviceHash            (익명 식별, 중복/악용 방지용)
  - trustScore (확장)
```

- **실시간성**: Firebase Realtime Database / Firestore 의 실시간 구독으로 좌석 상태를 푸시 (원본이 Firebase 기반이라 호환 용이)
- **자동 만료**: `expiresAt` 또는 하차역 order 도달 시 서버/클라이언트에서 제거
- **개인정보 최소화**: 계정 없이 `deviceHash` 만 사용, 위치/식별정보 저장 안 함

---

## 8. 기술 스택 제안

| 영역 | 제안 | 비고 |
|---|---|---|
| 클라이언트 | Flutter Web (PWA) | 원본과 동일 스택 → 자산·경험 재사용 |
| 대안 | React + TypeScript (PWA) | 웹 친화/SEO·채용 측면 고려 시 |
| 실시간 DB | Firebase Firestore / Realtime DB | 원본 Firebase 인프라 활용 |
| 호스팅 | Firebase Hosting | 원본과 동일 |
| 좌석 그리드 | SVG / Canvas 좌석 컴포넌트 | 호선별 레이아웃 데이터 주입 |
| 인증 | Firebase Auth (Google 로그인) | 구글 로그인 + 게스트 둘러보기 |
| 테마 | CSS 변수 프리셋 | 카톡/네이버/컬리/커스텀 |
| 노선도 | 카카오맵 지하철 노선도 연동 | 새 탭 바로보기 |
| 혼잡도 | TMAP/SK 칸 혼잡도 Open API | 칸별·역별 현황도 |

> 원본이 Flutter + Firebase 이므로 **동일 스택 유지**가 학습·재사용 측면에서 가장 빠른 경로다. 좌석 클릭 UI만 신규 컴포넌트로 추가하면 된다.

---

## 9. 신뢰도 · 악용 방지 정책

원본도 "이용자가 직접 등록하기 때문에 정확도는 달라질 수 있다"고 안내한다. 앉아가요는 이를 구조적으로 보완한다.

- **자동 만료**가 기본: 오래된 정보가 화면에 남지 않음
- **상태값으로 신선도 표현**: "다음 역/2~3정거장 후"로 시간 맥락 제공
- **신고/검증 버튼**: 실제와 다르면 다른 승객이 즉시 정정
- **신뢰도 점수(확장)**: 정확히 비운 이력이 쌓인 등록자의 정보를 우대
- **중복 등록 제한**: `deviceHash` 기준 한 좌석/시점당 1건
- **명확한 면책 안내**: "예약이 아닌 참고용" 문구를 메인에 상시 노출

---

## 10. 개발 로드맵 / 마일스톤

| 단계 | 기간(예시) | 산출물 |
|---|---|---|
| 0. 기획·리서치 | 1주 | 본 PRD, 호선별 좌석 레이아웃 데이터 수집 |
| 1. 좌석 그리드 프로토타입 | 1~2주 | 클릭 가능한 단순화 좌석 UI (정적 데이터) |
| 2. 실시간 연동 | 2주 | Firebase 등록/조회/자동만료 연결 |
| 3. MVP 통합·QA | 1~2주 | 컨텍스트 선택 + 그리드 + 공유/탐색 전 흐름 |
| 4. 베타 배포 | 1주 | PWA 배포, 1~2개 호선 한정 테스트 |
| 5. 피드백·확장 | 지속 | 신뢰도 점수, 다호선 확대, 알림 |

---

## 11. 성공 지표 (KPI)

- **활성도**: 출퇴근 시간대 일일 활성 사용자(DAU), 1인당 좌석 조회 수
- **공유 참여율**: 앉은 사용자 중 좌석을 등록한 비율
- **정보 정확도**: 등록 좌석이 실제로 비워진 비율(신고/검증 기반 추정)
- **재방문율**: 7일 리텐션 (출퇴근 반복 사용 특성)

---

## 12. 리스크 & 대응

| 리스크 | 대응 |
|---|---|
| 초기 사용자 부족 → 등록 정보 희박 | 특정 호선/시간대 집중 공략, 커뮤니티(예: 서울가이드) 협업 |
| 잘못된/장난 등록 | 자동 만료 + 신고 + 신뢰도 점수 |
| 좌석 점유 분쟁 우려 | "예약 아님" 명시, 좌석 잠금 기능 배제 |
| 호선별 좌석 배치 차이 | 레이아웃을 데이터로 분리, 단계적 호선 추가 |
| 개인정보/위치 민감성 | 계정·위치 비수집, 익명 기기 해시만 사용 |

---

## 12-B. 이용 모드 — 직접 참여 vs 뷰어

탑승 맥락(노선·열차·호차)을 확인한 뒤, 사용자는 두 가지 모드 중 하나를 고른다. 매번 공유할 필요 없이 "지금 내 칸 상황만 보고 싶을 때"를 위한 가벼운 진입점을 제공한다.

| 모드 | 목적 | 동작 |
|---|---|---|
| 🙋 직접 참여하기 | 내 자리·하차역을 공유하고 크레딧 적립 | 좌석 탭 → 하차역 선택 → 공유/보상, "앉지 못했어요" 가능 |
| 👀 뷰어 모드 | 지금 이 칸의 빈자리 상황만 확인 (공유 안 함) | 좌석 읽기 전용, 곧 비는 자리 개수 요약 + 새로고침, 자리 탭 시 상태만 음성/툴팁 안내 |

- 뷰어 모드에서는 좌석 등록이 일어나지 않아 데이터를 오염시키지 않고, 크레딧도 발생하지 않는다.
- 뷰어 화면에서 **"직접 참여로 전환"** 버튼으로 언제든 참여 모드로 바꿀 수 있다.
- "앉지 못했어요" → "곧 비는 자리 보기" 흐름도 자연스럽게 뷰어 모드로 연결된다.
- 접근성: 뷰어 요약("다음 역에서 비는 자리 N개")을 큰 글씨·음성으로 함께 안내한다.

## 13. 자동 실행 · 자동 컨텍스트 인식

사용자가 매번 노선·호차를 손으로 고르지 않도록, 탑승 맥락을 최대한 자동으로 채운다.

- **자동 실행 트리거(확장)**: 지하철 와이파이/비콘 접속, 또는 사용자가 등록한 출퇴근 시간·경로에 진입하면 알림으로 앱을 띄운다. (강제 실행이 아닌 "지금 공유할까요?" 제안 방식)
- **노선/열차 자동 인식**: 서울시 **실시간 열차 위치정보 API**로 해당 노선 운행 열차 목록을 받고, 단말 GPS·속도·진행방향으로 사용자가 탄 열차(`trainNo`)를 추정해 자동 매칭한다.
- **호차 입력**: 호차는 차내 게시 번호(예: `3012` 열차의 `4호차`)를 사용자가 한 번 탭하거나, NFC/QR(차내 부착 시) 인식으로 채운다.
- **하차역 후보 자동 정렬**: 현재 추정 위치 이후의 역만 큰 버튼으로 보여 선택을 최소화한다.

> 좌석 단위 점유 데이터는 어떤 공개 API에도 없다(그래서 크라우드소싱이 핵심). API는 "어느 노선·열차·역 부근인가"라는 **맥락 자동화**와 칸 단위 혼잡도에 쓰고, 좌석 정보는 사용자의 탭으로 채운다.

## 14. 실제 연동 API (서울시 공개 데이터)

| 용도 | API | 요청 형식 | 핵심 응답 필드 |
|---|---|---|---|
| 열차 위치(자동 인식) | 서울시 실시간 열차 위치정보 | `http://swopenapi.seoul.go.kr/api/subway/{KEY}/json/realtimePosition/0/200/{호선명}` | `trainNo`, `subwayNm`, `statnNm`, `updnLine`, `trainSttus`, `recptnDt` |
| 도착정보(하차 안내) | 서울시 실시간 도착정보 | `http://swopenapi.seoul.go.kr/api/subway/{KEY}/json/realtimeStationArrival/0/10/{역명}` | `subwayId`, `statnNm`, `trainLineNm`, `barvlDt`(도착예정초), `arvlMsg2` |
| 칸 혼잡도(보조 신호) | TMAP/SK 칸 혼잡도 Open API | 사업자 키 기반 REST | 칸별 혼잡도 등급 |

**인증·제약**

- 실시간 지하철 API는 **전용 인증키**를 별도 신청해야 한다(열린데이터광장 → 인증키 신청 → 실시간 지하철, 승인 1~2일).
- 호출 한도: 하루 약 **1,000건** → 서버에서 캐싱/프록시로 호출량을 관리한다.
- 서울시 외 구간 미제공 → 수도권 타 기관 API로 보완 필요.
- 엔드포인트가 `http`이므로 운영 시 **서버 프록시(HTTPS)**를 두고 키를 서버에 숨긴다(클라이언트 노출 금지).

**실제 응답 필드 (realtimePosition, 프로토타입 반영)**

| 필드 | 의미 |
|---|---|
| `subwayNm` | 노선명 (예: 2호선) |
| `trainNo` | 열차번호 (예: 3012) |
| `statnNm` | 현재 위치 역명 |
| `statnTnm` | 종착역명 (열차 행선지) |
| `updnLine` | 0 상행/내선, 1 하행/외선 |
| `trainSttus` | 0 진입, 1 도착, 2 출발, 3 전역 출발 |
| `recptnDt` | 정보 생성 시각 |

> 동봉한 프로토타입(`앉아가요_프로토타입.html`)에는 위 엔드포인트 호출 코드가 들어 있고, 발급받은 **실시간 인증키가 적용**되어 있다. 실행 시 선택한 노선의 **실시간 운행 열차 목록**을 불러와 사용자가 자기 열차를 고르는 흐름으로 동작한다.
>
> **단, 브라우저 직접 호출 주의**: 서울시 API는 `http`이고 CORS 헤더를 보장하지 않아, HTTPS 페이지/일부 브라우저에서 혼합콘텐츠·CORS로 차단될 수 있다. 이 경우 프로토타입은 자동으로 **샘플 열차로 폴백**한다. 운영에서는 반드시 **서버 프록시(HTTPS, 키 은닉)**를 둔다. 예: `GET /api/position?line=2호선` → 서버가 서울시 API를 호출해 JSON 중계. 키는 절대 클라이언트에 노출하지 않는다(현재 프로토타입은 데모 목적상 키가 파일에 노출되어 있으므로, 공개 배포 전 반드시 프록시로 이전·키 교체 권장).

## 15. 크레딧 보상 경제 (서로에게 베네핏)

좌석을 공유하는 선의에 보상을 줘서 참여를 유도한다. 양보 문화를 "주는 사람도 받는 사람도 이득"인 구조로 바꾼다.

- **앉았을 때(공유 성공)**: 좌석을 등록한 공유자에게 **기본 크레딧** 적립. 그 자리를 받아 앉게 된 탐색자에게도 **기본 크레딧**을 함께 지급(상호 보상).
- **앉지 못했을 때**: "앉지 못했어요"를 누르면 크레딧 없이, 대신 곧 비는 자리 안내로 연결(불이익 없음).
- **악용 방지**: 자동 만료·신고·`deviceHash` 중복 제한과 연동해, 허위 공유로는 크레딧이 확정되지 않도록 한다(검증 후 확정 모델).
- **크레딧 사용처(확장)**: 제휴 혜택, 뱃지/등급, 기부(약자석 캠페인) 등 — 현금성 보상은 지양해 어뷰징 유인을 낮춘다.

```
공유자 좌석 탭 → 공유 등록 →(검증)→ 공유자 +기본크레딧
                                  └→ 그 자리 앉은 탐색자 +기본크레딧
앉지 못함 → 크레딧 0, 곧 비는 자리 안내로 연결
```

## 16. 시니어 접근성 (완전 사용 가능하도록)

어르신도 도움 없이 쓸 수 있는 것을 1급 요구사항으로 둔다.

- **큰 글씨 모드**: 보통 / 큰 / 아주 큰 3단계 토글. 전체 폰트·버튼이 비례 확대.
- **음성 안내(TTS)**: 모든 단계에서 다음 할 일을 한국어 음성으로 안내(Web Speech API). 예: "윗줄 5번 자리를 선택했어요. 이제 내릴 역을 골라주세요."
- **음성 입력(확장)**: "강남에서 내려요" 같은 발화로 하차역 지정(SpeechRecognition).
- **단순 동선**: 한 화면에 한 가지 결정만. 큰 터치 타깃(최소 44pt 이상), 한 손 하단 조작.
- **명확한 색+라벨 병기**: 색약 대응(초록=다음 역, 노랑=2~3정거장, 파랑=내 자리, 보라=약자석).
- **읽기 쉬운 대비**: 고대비 다크 UI, 큰 숫자 좌석 라벨.

## 17. 사회적 목표 — 암묵지의 불편 해소

지하철 좌석 양보·자리 비움 정보는 지금까지 "눈치"라는 암묵지로만 오갔다. 누가 곧 내릴지, 어디가 비는지를 모두가 추측해야 했고, 정보가 특정 위치·사람에게 편중됐다. 앉아가요는 이 암묵지를 **모두가 보는 명시적 정보**로 바꿔, 좌석 정보의 편중과 불편을 줄이고, 거동이 불편한 분이 곧 비는 자리를 미리 알 수 있게 한다. 보상은 이 선순환을 지속시키는 장치다.

## 18. 로그인 — Firebase 구글 인증

- **방식**: Firebase Authentication의 **Google 제공자**(`signInWithPopup` + `GoogleAuthProvider`).
- **목적**: 크레딧·신뢰도·즐겨찾기 경로를 계정에 저장. 로그인 없이 **둘러보기(게스트)**도 허용해 진입 장벽을 낮춘다.
- **구현(프로토타입 반영)**: 로그인 화면 → 구글 버튼 → 인증 성공 시 프로필(이름/사진) 헤더 표시, 로그아웃 지원. `firebaseConfig`만 채우면 라이브 동작하며, 설정 전에는 데모 로그인으로 흐름을 확인할 수 있다.
- **설정 순서**: Firebase 콘솔에서 프로젝트 생성 → Authentication에서 Google 사용 설정 → 웹 앱 등록 후 SDK 설정값을 `firebaseConfig`에 입력 → 배포 도메인을 **승인된 도메인**에 추가(로컬 `file://`·미승인 도메인에서는 팝업 로그인이 차단됨).
- **데이터(확장)**: Firestore에 `users/{uid}` 문서로 크레딧·이력 저장, 보안 규칙으로 본인만 쓰기 허용.

## 19. 테마 — 카톡 / 네이버 / 컬리 / 커스텀

사용자가 익숙한 브랜드 톤을 고를 수 있게 4가지 테마를 제공한다. 선택은 브라우저에 저장되어 다음에도 유지된다.

| 테마 | 강조색 | 느낌 |
|---|---|---|
| 카톡 | 노랑 `#FEE500` | 밝은 크림 배경, 친근함 |
| 네이버 | 그린 `#03C75A` | 깔끔한 화이트 배경 |
| 컬리 | 퍼플 `#5F0080` | 차분한 라이트 퍼플 |
| 커스텀 | 사용자 지정 | 강조색 직접 선택 + 밝게/어둡게 |

- 구현은 CSS 변수(`--bg/--card/--ink/--accent/--accent-ink/--tint/--header-bg` 등) 프리셋 교체 방식이라 컴포넌트 수정 없이 즉시 반영된다.
- 강조색 대비를 계산해 버튼 글씨색을 자동으로 어둡게/밝게 맞춰 가독성을 보장한다(예: 카톡 노랑 위에는 어두운 글씨).
- 지하철 차량 일러스트는 모든 테마에서 동일한 다크 그래픽으로 유지해 좌석 색(초록/노랑/파랑/보라) 의미가 흔들리지 않게 한다.

## 20. 카카오 지하철 노선도 바로보기

- 헤더 **🗺️ 버튼 → "카카오 지하철 노선도 열기"**로 카카오맵 지하철 노선도(`https://map.kakao.com/?target=subway&REGION=01`)를 새 탭에서 연다.
- 카카오맵은 외부 `iframe` 임베드를 제한하므로 **새 탭 열기** 방식을 사용한다(임베드 대신 안정적 연결).
- 카카오맵의 초정밀 지하철 서비스는 실시간 열차 위치·도착시간을 제공하므로, 앉아가요의 좌석 정보와 상호 보완된다.

## 21. 실시간 혼잡도 현황도

좌석 정보와 함께 "어느 칸·어느 구간이 붐비는지"를 한 화면에서 보여준다.

- **칸별 혼잡도**: 현재 열차 10개 칸을 막대그래프로 표시(여유/보통/혼잡/매우혼잡 4단계 색). 가장 한산한 칸을 안내해 분산 탑승을 유도 → **좌석 편중 완화**라는 목표와 직접 연결된다.
- **노선 역별 혼잡도**: 현재역 이후 정차역들의 혼잡도를 색 띠로 표시.
- **데이터 소스(운영)**: TMAP/SK의 **칸 혼잡도 Open API**(진입 역 기준 실시간 칸 혼잡도)와 서울교통공사 혼잡도 데이터를 연동. 프로토타입은 데모 값으로 동작하며 API 연결 지점이 분리돼 있다.
- 접근성: 혼잡도 요약("○호차가 가장 한산해요")을 큰 글씨·음성으로 함께 안내.

---

## 부록 A. 원본 대비 한눈에 보는 차이

| 구분 | 저내려요 (원본) | 앉아가요 (업그레이드) |
|---|---|---|
| 좌석 등록 | 텍스트/리스트 입력 | 좌석 그리드 **탭** |
| 위치 직관성 | 낮음 | 높음 (도면 위 하이라이트) |
| 신선도 표현 | 약함 | 상태값 + 카운트다운 + 자동만료 |
| 신뢰도 | 등록자 성실도 의존 | 점수·신고·검증 |
| 맥락 입력 | 수동 | 실시간 위치 API로 자동 인식 |
| 참여 동기 | 선의 | 상호 크레딧 보상 |
| 접근성 | 일반 | 큰 글씨 3단계 + 음성 안내 |
| 철학 | 참고용 공유 | 동일 (참고용, 예약 아님) |

## 부록 B. 다음 작업 제안

1. 호선별 실제 좌석 배치(일반석/약자석 수) 데이터 수집 — 1호선·2호선 우선
2. 본 PRD 기반 좌석 그리드 **클릭 프로토타입**(HTML/Flutter) 제작
3. 와이어프레임 → 디자인 시안(피그마) 작성

---

*본 문서는 '앉아가요' 앱 개발을 위한 초기 기획(준비) 단계 산출물입니다. 수치·기간은 예시이며 검증을 거쳐 조정합니다.*
